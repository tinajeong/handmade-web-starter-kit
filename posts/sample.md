별도의 `.md` 파일로 관리하니 글 쓰기가 훨씬 편해졌습니다.

## 코드 하이라이팅 테스트

물론 **Highlight.js**도 여전히 잘 작동합니다.

```javascript
// manifest.json을 불러오는 예시
fetch('manifest.json')
  .then(response => response.json())
  .then(data => console.log(data));
```
